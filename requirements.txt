# Versions used by the current verified environment.
# Install LLaMA-Factory at the pinned commit before applying the patches.
torch==2.6.0
transformers==5.7.0
peft==0.18.1
accelerate==1.11.0
datasets==4.0.0
trl==0.24.0
PyYAML
